# 当前预实验结果与列生成算法瓶颈诊断

> **归档说明（2026-08-26）**：本文记录的是 V5 开发前的
> `integrated_zone_v4` 基线结果，只用于历史对照。当前开发主线已经
> 进入 V6 row-aware bay-zone 模型；V6 的 zone 定义、可行域和目标均已
> 改变，所以本文全部 UB/LB/gap 都不能作为 V6 结果或 V6 基线。

更新时间：2026-08-25  
模型版本：`integrated_zone_v4`  
代码分支：`feat/integrated-yard-objective`

## 1. 文档目的

本文档回答三个问题：

1. 当前模型修改后，列生成算法与完整排区 MIP 的实际对比结果是什么；
2. 为什么当前算法只在部分规模上体现 UB 和 gap 优势；
3. 后续如果希望在较少箱组时也取得稳定、明显的优势，应优先解决哪些代码瓶颈。

本文档中的“完整 MIP”是与列生成算法使用同一 `integrated_zone_v4` 连续排区模型的全枚举排区 MIP，不是排级 M0，也不是旧大计划模型。两种方法使用相同输入、seed、线程数、时间预算、目标函数和精确排级 recourse，因此 UB 可以直接比较。

## 2. 结果版本边界

当前模型相对 v3 已发生两项会改变目标值的修改：

- 箱组跨箱区数量退出主目标，仅保留为诊断指标；
- 原权重按比例重新分配到其余五个目标项。

因此：

- v3 与 v4 的 UB、LB、gap 不能直接相减；
- v3 结果只能用于观察历史上的规模趋势；
- 本文的当前数值结论全部采用新补跑的 v4 配对结果。

## 3. 实验设计

### 3.1 公平性设置

- 求解器：Gurobi；
- 线程数：1；
- `PYTHONHASHSEED=0`；
- 4、16、24 箱组：每种方法 60 秒；
- 48、72、96 箱组：每种方法 120 秒；
- 输入：相同堆场快照和相同场景生成清单；
- 72、96 箱组包含确定性的航次异质性，不是简单复制完全相同的航次；
- 所有列生成结果和完整 MIP 结果均通过内部校验和独立输出校验。

### 3.2 指标定义

目标为最小化。本文使用：

\[
\text{UB improvement}
=\frac{UB_{MIP}-UB_{CG}}{|UB_{MIP}|}.
\]

该值为正表示列生成算法获得了更好的整数解。

列生成算法的独立认证 gap 为：

\[
\text{gap}_{CG}
=\frac{UB_{CG}-LB_{root}}{|UB_{CG}|},
\]

其中 `LB_root` 是精确定价闭合后的完整排区 LP 根节点下界。

完整 MIP 的 gap 使用其分支定界过程中得到的全局下界。为了把“UB 搜索能力”和“LB 认证能力”分开，本文还报告：

\[
\text{common-bound gap}_{CG}
=\frac{UB_{CG}-LB_{MIP}}{|UB_{CG}|}.
\]

该指标仅用于实验诊断，因为它借用了对比算法产生的有效下界，不能作为列生成算法独立运行时报告的 gap。

## 4. v4 完整配对结果

| 算例 | 箱组 | 排区数 | CG UB | CG 根 LB | CG 独立 gap | MIP UB | MIP LB | MIP gap | CG 公共下界 gap | CG UB 改善 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| `pilot_s_101` | 4 | 7,540 | 0.050605 | 0.035391 | 30.06% | 0.050605 | 0.045624 | 9.84% | 9.84% | 0.00% |
| `pilot_m_201` | 16 | 30,251 | 0.061190 | 0.050174 | 18.00% | 0.061648 | 0.054500 | 11.60% | 10.93% | +0.74% |
| `pilot_l_301` | 24 | 40,054 | 0.135016 | 0.115627 | 14.36% | 0.130387 | 0.120011 | 7.96% | 11.11% | -3.55% |
| `scale_g048_s601` | 48 | 79,892 | 0.139063 | 0.118305 | 14.93% | 0.145105 | 0.119862 | 17.40% | 13.81% | +4.16% |
| `scale_g072_s701` | 72 | 119,944 | 0.141710 | 0.119407 | 15.74% | 0.147382 | 0.120132 | 18.49% | 15.23% | +3.85% |
| `scale_g072_s702` | 72 | 119,980 | 0.130489 | 0.108538 | 16.82% | 0.132871 | 0.109254 | 17.77% | 16.27% | +1.79% |
| `scale_g096_s801` | 96 | 159,907 | 0.166001 | 0.139889 | 15.73% | 0.167890 | 0.140260 | 16.46% | 15.51% | +1.13% |
| `scale_g096_s802` | 96 | 159,924 | 0.147435 | 0.124733 | 15.40% | 0.152143 | 0.125335 | 17.62% | 14.99% | +3.09% |

### 4.1 总体结果

全部 8 个算例中：

- 列生成算法获胜 6 次；
- 完整 MIP 获胜 1 次；
- 两者持平 1 次；
- 列生成 UB 平均改善 1.40%，中位数改善 1.46%；
- 列生成独立 gap 平均为 17.63%，完整 MIP 平均为 14.64%。

最后一项说明：虽然列生成的 UB 胜率较高，但当前仍不能声称它在所有规模上的独立认证 gap 优于 MIP。4、16、24 箱组的根节点下界劣势拉高了总体平均值。

### 4.2 48 箱组及以上

48、72、96 箱组共 5 个算例中：

- 列生成算法 5 次全部获得更低 UB；
- UB 平均改善 2.80%，范围为 1.13%–4.16%；
- 列生成独立 gap 平均为 15.72%；
- 完整 MIP gap 平均为 17.55%；
- 列生成独立 gap 平均低 1.82 个百分点；
- 使用共同 MIP 下界时，列生成 gap 平均为 15.16%，比 MIP低 2.39 个百分点。

因此，当前 v4 已经在 48 箱组及以上显示出可重复的规模优势，但“明显优势”仍不稳定：最小 UB 优势只有 1.13%，不同种子之间波动较大，而且没有随着 48→72→96 箱组单调扩大。

### 4.3 4–24 箱组

- 4 箱组：两种方法 UB 相同；列生成受限整数主问题已求到最优，但独立 gap 仍有 30.06%；
- 16 箱组：列生成 UB 领先 0.74%，但独立 gap 比 MIP 高 6.40 个百分点；
- 24 箱组：列生成 UB 落后 3.55%，独立 gap 比 MIP 高 6.40 个百分点。

这组结果把中小规模问题分成两个来源：

1. 4、16 箱组主要是 LB 认证问题；
2. 24 箱组同时存在 UB 搜索问题和 LB 认证问题。

## 5. 根节点下界的规模特征

完整 MIP 经过分支定界后，相对列生成根 LP 下界的提升如下：

| 算例 | MIP LB 相对根 LP LB 的提升 |
|---|---:|
| 4 箱组 | 28.91% |
| 16 箱组 | 8.62% |
| 24 箱组 | 3.79% |
| 48 箱组 | 1.32% |
| 72 箱组 seed 701 | 0.61% |
| 72 箱组 seed 702 | 0.66% |
| 96 箱组 seed 801 | 0.27% |
| 96 箱组 seed 802 | 0.48% |

这说明：

- 当前精确定价确实闭合了完整排区 LP，但 LP 松弛在小规模上存在明显整数性缺口；
- 完整 MIP 在小规模上能够通过分支获得远强于根 LP 的全局下界；
- 规模增大后，完整 MIP在规定时间内几乎无法把下界提高到根 LP 之上，列生成的 LB 劣势随之消失；
- 当前列生成的 gap 优势主要依赖“大规模时完整 MIP 来不及分支”和“列生成取得更好 UB”，而不是列生成自身拥有更强的整数下界机制。

## 6. 根列池和时间增长

| 箱组 | 隐式排区数 | 根节点活动排区 | 活动比例 | 根轮数 | 根耗时 | 总耗时 | 受限整数主问题状态 |
|---:|---:|---:|---:|---:|---:|---:|---|
| 4 | 7,540 | 142 | 1.88% | 7 | 0.17 s | 7.08 s | optimal |
| 16 | 30,251 | 764 | 2.53% | 15 | 0.99 s | 57.28 s | timelimit |
| 24 | 40,054 | 1,065 | 2.66% | 13 | 1.25 s | 57.42 s | timelimit |
| 48 | 79,892 | 4,487 | 5.62% | 17 | 4.01 s | 114.68 s | timelimit |
| 72/701 | 119,944 | 8,802 | 7.34% | 20 | 9.22 s | 114.98 s | timelimit |
| 72/702 | 119,980 | 8,791 | 7.33% | 19 | 8.64 s | 114.98 s | timelimit |
| 96/801 | 159,907 | 19,801 | 12.38% | 29 | 26.88 s | 115.42 s | timelimit |
| 96/802 | 159,924 | 18,648 | 11.66% | 29 | 33.65 s | 115.44 s | timelimit |

根节点虽然全部严格闭合，但活动排区比例和根耗时随规模快速增长。96 箱组只比 72 箱组多 33% 的箱组，根活动列却增加到约 2.1–2.25 倍，根耗时增加到约 3–3.9 倍。

这会直接挤压后续整数搜索和 Fix-and-Optimize 的时间，也解释了为什么 96 箱组 UB 优势没有稳定扩大。

## 7. 当前目标函数的预实验信息

当前五项目标权重与 72/96 箱组四个算例的实际加权目标占比如下：

| 目标项 | 设置权重 | 实际目标值占比 |
|---|---:|---:|
| 航次跨箱区 | 15.00% | 13.07% |
| 不连续排区数量 | 35.00% | 15.05% |
| 靠近已有同类箱 | 12.50% | 33.16% |
| 未利用排区容量 | 21.25% | 2.73% |
| 泊位—箱区距离 | 16.25% | 36.00% |

目标值占比表示优化后仍残留的惩罚，不等于边际作用。未利用容量消融给出了更直接的因果证据：

| 指标（四算例平均） | 保留未利用容量目标 | 删除该目标 |
|---|---:|---:|
| 未利用排区容量 | 42.50 箱 | 228.75 箱 |
| 额外排区数 | 137.75 | 137.25 |
| 航次额外箱区数 | 44.00 | 35.50 |
| 箱组额外箱区数（诊断） | 80.00 | 73.25 |
| 泊位距离原始量 | 727.57 | 736.36 |

删除后未利用容量增加到 5.38 倍，而排区数几乎不变，因此这一项不能由“不连续排区数量”替代，当前证据支持保留它。

## 8. 当前算法的主要不足和瓶颈

### 8.1 缺少根节点之后的全局整数下界机制

当前流程在根节点做精确定价，闭合后得到完整排区 LP 的全局下界；之后只在受限列池上求整数解并执行局部改进。受限列池 MIP 的 bound 不是完整模型的全局 bound，因此最终仍只能报告根 LP 下界。

这会导致：

- 即使 4 箱组的受限整数主问题已经最优，独立 gap 仍为 30.06%；
- 小规模完整 MIP通过分支定界显著提高 LB，而当前方法无法跟随；
- 只改善 UB 无法从根本上消除小规模 gap 劣势。

如果论文要求列生成算法在独立认证 gap 上稳定优于完整 MIP，最终需要更强的有效不等式、分支定价，或其他能够产生完整模型全局整数下界的机制。仅增加根节点定价列数不会解决这个问题，因为当前根节点已经精确闭合。

### 8.2 LP 最优列不一定是好的整数列

根定价和整数池扩充都主要依据根 LP 对偶信息。当前整数池扩充采用每箱组 `min(100, ceil(sqrt(平均排区数)))` 的列数限制，并继续按根对偶检验数选列。

这种策略适合恢复 LP 最优性，但不一定包含构成优质整数解所需的列：

- 某些列在根 LP 下检验数不突出，却能减少整数排区碎片；
- 某些列只有与其他箱组联合换位时才有价值；
- 根 LP 可以分数使用多个排区，掩盖单个完整排区的容量浪费和资源冲突；
- 泊位距离、已有箱邻近、未利用容量和峰值利用率共同作用时，单一对偶排序可能缺少多样性。

24 箱组 UB 落后 3.55%，以及 96 箱组部分种子优势缩小，是这一瓶颈的直接风险信号。

### 8.3 初始受限 MIP消耗大量时间，但其证明价值不能转化为全局 gap

当前时间策略默认：

- 根节点最多使用剩余时间的 50%；
- 初始受限整数主问题使用之后剩余时间的 75%；
- Fix-and-Optimize 只使用再剩余的部分；
- 最后预留总预算的 5% 做排级精确回填。

除 4 箱组外，所有初始受限整数主问题均以 `timelimit` 结束。即便求解器继续提高受限模型 bound，该 bound 也不能作为完整模型全局 LB。因此，当前时间分配可能过度投入“关闭受限 MIP”，而对真正决定 UB 的多样化搜索和邻域改进投入不足。

### 8.4 Fix-and-Optimize 只有一次、一个邻域

当前只执行一次目标贡献驱动的 Fix-and-Optimize：

- 选择至少覆盖 60% 可归因目标的箱组；
- 候选排区总量尽量限制在完整排区数的 35%；
- 其余箱组的排区和流量被固定；
- 对入选箱组一次性物化全部排区并求一个局部 MIP。

其不足包括：

- 没有多轮迭代，也没有在改进后重新选择邻域；
- 只按当前目标贡献排序，没有显式考虑资源冲突图、航次耦合或容量交换关系；
- 高贡献箱组可能不是阻塞改进的箱组；
- 候选排区多的箱组容易受 35% 预算限制而被跳过；
- 一次性物化邻域内全部排区会增加建模成本，挤压求解时间。

### 8.5 峰值利用率约束使局部移动需要协调交换

72/96 箱组四个基准解的峰值利用率余量分别只有：

- 0.00190；
- 0.00177；
- 0.00126；
- 0.00631。

这说明峰值利用率 ε 约束在大型算例中几乎绑定。一个箱组单独迁移到更近或更集中的箱区时，往往必须同时释放该箱区中的其他负载。因此，按单箱组目标贡献选择邻域可能无法包含完整的交换链。

### 8.6 根节点计算开始成为规模瓶颈

精确 RMQ 定价的正确性目前没有问题，所有算例都闭合。但 96 箱组根耗时已达到 26.9–33.6 秒，并产生约 1.86–1.98 万个活动排区。继续扩大数据后可能出现：

- 根节点超过自适应时间包络，算法无法报告全局 LB；
- 根列池本身变得足够大，使受限 MIP失去规模优势；
- 后续邻域搜索时间进一步减少。

### 8.7 排级 recourse 不是当前主要瓶颈

完整 MIP 配对实验中的精确排级 recourse 耗时约 0.15–1.39 秒，远低于主求解时间，并且全部通过构造证书、内部校验和外部校验。

因此，现阶段不应优先简化排级 recourse。真正影响 UB 和 gap 的是排区主问题的列池、整数搜索、邻域结构和全局下界。

### 8.8 实验记录还缺少关键过程指标

当前 suite 汇总保留了根耗时、根轮数和最终 UB/LB，但没有完整保存：

- 初始受限 MIP 的首解时间、最好解时间和 UB 轨迹；
- 初始 MIP实际耗时、节点数、最终受限 bound；
- greedy start 是否完整、起始目标值及被 Gurobi 接受情况；
- Fix-and-Optimize 选中箱组数、目标质量覆盖率和候选预算是否绑定；
- 局部 MIP建模时间、求解时间和相对改善；
- 最终主问题重新优化的贡献；
- 各阶段未使用的剩余时间。

在缺少这些数据时，直接修改列数、时间比例或邻域大小容易把结果变化误判为算法结构改善。

## 9. 后续代码优化优先级

### P0：先补过程诊断，不改变数学模型

1. 将 `zone_mip_initial`、`zone_fix_optimize` 和各阶段时间写入预实验汇总；
2. 记录 time-to-first、time-to-best、节点数和 UB 轨迹；
3. 记录根列、整数扩充列和 greedy seed 列各自被最终解采用的数量；
4. 记录邻域选择是否被目标质量阈值或候选预算截断。

这是后续所有参数和结构修改的共同基础。

### P1：优先提高 UB

1. 将整数池从“单一根检验数排序”改为多准则、多样化选列；
2. 同时保留低检验数列、低未利用容量列、低距离列、已有箱邻近列和资源冲突互补列；
3. 生成多个不同箱组顺序、不同进口保护策略的可行 MIP start；
4. 对初始受限 MIP采用以尽快改进 incumbent 为目标的停止策略，不再试图把受限模型求到零 gap；
5. 把单次 Fix-and-Optimize 改成多轮自适应邻域；
6. 邻域选择同时考虑目标贡献、共享物理资源、同航次关系和峰值利用率交换链；
7. 每轮根据剩余时间和上一轮改善动态扩大或缩小邻域。

这一阶段最有希望把优势提前到 16/24 箱组，并提高 72/96 箱组优势的稳定性。

### P2：加强独立 LB 和 gap

先从比完整 branch-and-price 风险更低的方向开始：

1. 分析航次箱区激活、箱组排区激活和流量连接约束的 LP 分数结构；
2. 增加经过独立验证的 cover、clique、资源冲突和激活下界类有效不等式；
3. 在微型完整枚举算例上验证每类 cuts 的有效性和根 gap 改善；
4. 评估 cuts 是否会使定价子问题失去现有 RMQ 可解结构。

如果加强根模型后，小规模独立 gap 仍明显落后，而论文又必须强调严格 gap 优势，则需要评估真正的 branch-and-price。它必须在分支节点继续精确定价并维护全局树下界，开发和正确性验证成本显著高于当前 matheuristic。

### P3：控制根节点扩张和动态分配时间

1. 对定价对偶做稳定化，减少相近列反复进入；
2. 检查 96 箱组活动列比例快速上升的具体约束和箱组；
3. 对定价状态、区间信息和列系数进行缓存；
4. 根节点提前闭合时把全部剩余预算返还整数搜索；
5. 根据根耗时、列池大小和 incumbent 改善速度动态决定初始 MIP与邻域时间；
6. 精确回填完成后如仍有预算，允许继续主问题改进，而不是直接结束。

## 10. 建议的阶段门标准

为了避免只凭一两个种子判断“明显优于”，建议在代码优化前固定阶段门。可采用以下暂定标准：

- 同输入、同 seed、同线程、同总时间；
- 16、24、48、72、96 箱组每个规模至少 5 个种子；
- 关注 UB 改善中位数，而不是只看平均值；
- 目标：48 箱组及以上 UB 改善中位数至少 3%，胜率至少 80%；
- 目标：16/24 箱组不再出现超过 1% 的系统性 UB 劣势；
- 目标：48 箱组及以上独立 gap 平均至少比完整 MIP低 2 个百分点；
- 单独报告根 LP gap、共同下界 gap和各自独立 gap；
- 所有结果必须通过同一套内部与外部校验。

4 箱组的完整 MIP规模很小，要求列生成在 UB 上明显领先并不现实；更合理的目标是小算例用于正确性和下界验证，把算法优势的交界点从当前约 48 箱组提前到 16–24 箱组。

## 11. 当前结论

当前 v4 算法已经具备可发表研究的基础特征：

- 根节点定价严格闭合；
- 同模型输入和输出可以独立验证；
- 48 箱组及以上已表现出稳定的 UB 胜率和平均 gap 优势；
- 排区数增长时完整 MIP 的规模劣势开始出现。

但它还没有达到“在 UB 和 gap 上明显优于 MIP”的目标：

- 24 箱组 UB 仍落后；
- 16 箱组优势过小；
- 大型算例 UB 优势在 1.13%–4.16% 之间波动；
- 小规模独立 gap 被根 LP 整数性缺口明显拖累；
- 当前只有一个初始整数池、一个主要 MIP和一次 Fix-and-Optimize；
- 缺少分支节点级的全局下界改进机制。

因此，下一步不应先大幅改变目标函数，也不应优先修改排级 recourse。最合理的路线是：先增加阶段诊断，再优先提高 UB 的整数列池和多轮邻域搜索，随后根据根 gap 实验决定是否加入强化 cuts 或 branch-and-price。

## 12. 实验结果位置

当前 v4 列生成结果：

- `preexperiment_outputs/integrated_v4_pilot_60s_20260825/`
- `preexperiment_outputs/integrated_v4_scale_g048_120s_20260825/`
- `preexperiment_outputs/integrated_v4_unused_baseline_scale_120s_20260825/`

当前 v4 完整 MIP 配对结果：

- `preexperiment_outputs/integrated_v4_pilot_complete_mip_60s_20260825/`
- `preexperiment_outputs/integrated_v4_scale_g048_complete_mip_120s_20260825/`
- `preexperiment_outputs/integrated_v4_scale_complete_mip_120s_20260825/`

当前 v4 未利用容量消融结果：

- `preexperiment_outputs/integrated_v4_unused_ablation_scale_120s_20260825/`

`preexperiment_outputs/` 已被 Git 忽略；正式论文实验应另外建立包含配置、汇总表和环境信息的可追溯结果归档策略。
